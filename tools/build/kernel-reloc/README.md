Kernel Relocation Extraction Tool
=================================

Tool to extract kernel relocation sections emitted during build. 

These variables/pointers that need to be patched during runtime will
be added to a TLV in the kernel. 